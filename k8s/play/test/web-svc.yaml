apiVersion: v1
kind: Service
metadata:
  name: web-flask-svc
  labels:
    app: web-flask
spec:
  type: LoadBalancer
  ports:
  - port: 3000
    nodePort: 30036
    targetPort: 5000
  selector:
    app: web-flask
    version: v1